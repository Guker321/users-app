import React from 'react';

import classes from './Loading.module.scss';

const Loading: React.FC = () => <div className={classes.component}><div className={classes.spinner} /></div>;

export default Loading;
